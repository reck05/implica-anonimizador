streamlit>=1.31.0
pandas>=2.0.0
typer>=0.12.0
rich>=13.7.0
openpyxl>=3.1.2
python-docx>=1.1.0
python-pptx>=0.6.23
pymupdf>=1.24.0
spacy>=3.7.0,<4.0.0
rapidfuzz>=3.6.0
pydantic>=2.6.0

# El modelo es_core_news_md se descarga en runtime la primera vez (ver detectors.py).
# No lo añadimos aquí como wheel URL porque algunos hosts (Streamlit Cloud, etc.)
# bloquean URLs externas en requirements.txt y falla el build entero.
